name: CI - Check Working Repo

on:
  push:
    branches: [ dev, docker_cicd ]
  workflow_dispatch:

jobs:
  check_working_repo:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Set up Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.10'

      - name: Install dependencies
        run: |
          pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run training
        run: python train.py

      - name: Run testing
        run: python test.py